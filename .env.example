# Drizzle
DATABASE_URL=local.db

# OpenRouter API for AI-powered specification parsing
OPENROUTER_API_KEY=sk-or-v1-...

# Temporary storage directory for PDF parsing (optional, defaults to /tmp/planck-parse)
# Used to store uploaded PDFs and extracted text during the multi-step parsing process
# Files are automatically cleaned up after 1 hour
PARSE_TEMP_DIR=/tmp/planck-parse
